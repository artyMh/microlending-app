import React from 'react';
import { mount } from 'enzyme';

import Calculator from '../../src/components/calculator';

describe('Calculator component', () => {
    // Sorry, had not enough time for them, but you can look my other tests at other project here:
    // https://github.com/artyMh/test-app/tree/master/client/test Frontend tests
    // https://github.com/artyMh/test-app/tree/master/server/test Backend tests
    test('empty test', () => {});
});